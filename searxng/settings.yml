use_default_settings: true

server:
  # Generate a real secret for production: openssl rand -hex 32
  secret_key: "nemoclaw-local-dev-secret"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json

engines:
  - name: google
    engine: google
    shortcut: g
  - name: duckduckgo
    engine: duckduckgo
    shortcut: ddg
  - name: bing
    engine: bing
    shortcut: b
